---
engine: julia
lang: es-MX

---
```{julia}

using CSV, DataFrames, Downloads, StatsBase
using PlotlyLight

```

```{julia}
URL_CSV = "https://raw.githubusercontent.com/plotly/datasets/master/iris.csv"

Downloads.download(URL_CSV, "iris.csv")
df = CSV.read("iris.csv", DataFrame)
```

```{julia}
describe(df)
```

```{julia}
df_setosa = filter(r -> r.Name == "Iris-setosa", df)
```

```{julia}
using PlotlyLight

plot(x=1:100, y=cumsum(rand(100)))
```


```{julia}
#| label: fig-example
#| fig-cap: "An example Plotly plot from Julia"

p = PlotlyLight.Plot()
p.layout.title = "My Plotly Light Plot"
p.data = [
    PlotlyLight.Config(
        x = [1, 2, 3],
        y = [4, 1, 2],
        mode = "lines+markers",
        name = "Series 1"
    )
]
p
```